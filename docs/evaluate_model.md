## Model evaluation

**Goal**: You need to write a data loader that loads video frames from selected timestamps and extracts the corresponding latents from these frames. Then, you need to align these video latents with the neural data for encoding and decoding.

TODO
